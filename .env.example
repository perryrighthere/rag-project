# Application
APP_NAME=rag-project
APP_ENV=local
API_PREFIX=

# Database
# Local development default. The generated database file is ignored by git.
DATABASE_URL=sqlite+pysqlite:///./rag_project.db
# PostgreSQL example for production-like deployments:
# DATABASE_URL=postgresql+psycopg://rag:rag@localhost:5432/rag

# MinIO
MINIO_ENDPOINT=localhost:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=rag-project
MINIO_SECURE=false
MINIO_PUBLIC_ENDPOINT=http://localhost:9000

# MinerU API
MINERU_BASE_URL=http://localhost:18000
MINERU_BACKEND=pipeline
MINERU_PARSE_METHOD=auto
MINERU_LANG_LIST='["ch"]'
MINERU_REQUEST_TIMEOUT=60
MINERU_POLL_INTERVAL_SECONDS=2
MINERU_MAX_WAIT_SECONDS=600

# Optional VLM image explanations
VLM_IMAGE_EXPLANATIONS_ENABLED=false
VLM_BASE_URL=
VLM_API_KEY=EMPTY
VLM_MODEL=
VLM_PROMPT=
VLM_TIMEOUT=120
VLM_MAX_TOKENS=300

# Embedding API, OpenAI-compatible
EMBEDDING_BASE_URL=https://your-openai-compatible-provider.example.com/v1
EMBEDDING_API_KEY=your-api-key
EMBEDDING_MODEL=text-embedding-model
EMBEDDING_DIM=1024
EMBEDDING_BATCH_SIZE=32
EMBEDDING_TIMEOUT=60

# Milvus
MILVUS_URI=http://127.0.0.1:19530
MILVUS_COLLECTION=rag_chunks

# Optional rerank API, OpenAI-compatible
RERANK_BASE_URL=
RERANK_API_KEY=EMPTY
RERANK_MODEL=
RERANK_TIMEOUT=60

# Chat API, OpenAI-compatible
CHAT_BASE_URL=https://your-openai-compatible-provider.example.com/v1
CHAT_API_KEY=your-api-key
CHAT_MODEL=chat-model
CHAT_TIMEOUT=60
CHAT_TEMPERATURE=0.2
CHAT_MAX_TOKENS=1200
